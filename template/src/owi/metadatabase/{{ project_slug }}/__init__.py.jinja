"""{{ project_description }}.

This package extends the ``owi.metadatabase`` namespace with the
``{{ project_slug }}`` extension.
"""

from .io import {{ project_name | replace('-', ' ') | title | replace(' ', '') }}API

__version__ = "0.1.0"

__all__ = ["{{ project_name | replace('-', ' ') | title | replace(' ', '') }}API", "__version__"]
