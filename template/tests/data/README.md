# Place CSV data files for integration tests here.
#
# Each file should be named after the database table it targets, e.g.:
#   records.csv  →  loaded into the `records` table
#
# Files must include a header row matching the column names of the target table.
